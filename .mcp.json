{
  "mcpServers": {
    "axe-mcp-server": {
      "command": "sh",
      "args": [
        "-c",
        "export AXE_ACCESS_TOKEN=\"$(npx -y @deque/axe-auth token 2>/dev/null)\"; exec docker run -i --rm -e AXE_API_KEY -e AXE_ACCESS_TOKEN -e AXE_SERVER_URL dequesystems/axe-mcp-server:latest"
      ]
    }
  }
}
